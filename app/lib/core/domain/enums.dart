enum PbEvent {
  m800,
  m1500,
  m3000,
  m3000sc,
  m5000,
  m10000,
  half,
  full,
}

enum Zone { E, M, T, I, R }

enum RestType { stop, jog }

enum SessionStatus { done, partial, aborted, skipped }

